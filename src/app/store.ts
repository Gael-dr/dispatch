// Store configuration
// This file can be extended with Redux, Zustand, or any other state management solution

export type AppState = Record<string, unknown>

export const initialState: AppState = {
  // Initial state
}
